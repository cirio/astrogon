---
import BaseLayout from "@components/base/BaseLayout.astro";
import { markdownify } from "@lib/textConverter";
import Button from "@components/common/Button.astro";
import type { HomeEntry } from "@/types";
import { Image } from "astro:assets";

interface Props {
  entry: HomeEntry;
}

const { entry } = Astro.props;
const { image, imageAlt, title, subtitle, content, button } = entry.data;
---

<BaseLayout>
  <!-- Banner -->
  <section class="section">
    <div class="container">
      <div class="row justify-center">
        <div class="text-center lg:col-6">
          {
            image && (
              <div class="blur-[0px]">
                <Image
                class="mb-6 w-64"
                src={image}
                alt={imageAlt}
                width={1024}
                height={1024}
                loading="eager"
              />
              </div>
              
            )
          }
          <div class="glass rounded-lg p-4 mb-8">
            <h1 set:html={markdownify(title)} class="font-black" />
            {subtitle && <p set:html={markdownify(subtitle)} class="mb-4 text-base" />}
            <p set:html={markdownify(content)} class="font-black" />
          </div>
          {
            button && (
              <Button
                label={button.label}
                link={button.link}
                newtab
                hoverInvert
                color=""
                class="font-black"
              />
            )
          }
        </div>
      </div>
    </div>
  </section>
</BaseLayout>
